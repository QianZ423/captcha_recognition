当模型的准确率较低的时候，这个时候就需要做优化了

方法：
1、适当增加模型的复杂度，例如增加隐藏层的个数
2、可能是因为激活函数sigmod产生了梯度消失的问题，这个时候要么换用激活函数，要么就做批归一化tf.contrib.layers.batch_norm()


注：小批量梯度下降方法是提高运算速度的方法
CNN算法加入了“局部视野感知”，一块一块看，这就是人类的先验知识，而DNN就是一个像素点一个像素点的处理，每个像素点都是wx+b


批归一化：就是把数据全部缩放到某个区间，比如0-1区间内，防止有的数据过大，有的数据过小

为了防止过拟合，每次训练模型的时候可以生成不同的100条数据
